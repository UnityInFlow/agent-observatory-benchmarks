# opencode review — 20260903T081147Z

| | |
|---|---|
| head | `987b059` |
| base | `8aadc75` |
| model | `ollama-cloud/glm-5.2` |
| agent | `bench-critic` |
| reviewed | 2 of 2 |

[0m
> bench-critic · glm-5.2
[0m
[0m→ [0mRead .claude/hooks/opencode-review.sh
[0m→ [0mRead .claude/hooks/opencode-review.test.sh
[0m
[0m$ [0mrtk read .gitignore 2>/dev/null; echo "---"; rtk ls -la .claude/ 2>/dev/null; echo "---"; git ls-files .claude/ 2>/dev/null
target/
*.log
evaluation*.json
run*.json

# Ambient tooling artifacts. Background daemons, editors and indexers on a developer's
# machine write into whatever directory they are pointed at — including a benchmark
# worktree. Anything matched here is environment noise, not agent output, and must never
# be counted as a scope violation by the evaluator.
.DS_Store
.memdb/
.idea/
.vscode/
*.pid
*.swp
# Note the trailing `/*` rather than `/`: git will not re-include anything inside a directory
# it has already excluded, so `.claude/` plus `!.claude/hooks/` silently ignores the hook and
# leaves the repo looking as though it has a reviewer it never committed.
.claude/*
# ...except the committed review hook. These two are TRACKED, so the evaluator's scope guard
# sees them only when an agent actually edits them — which is a real violation and should
# fail AC6. Everything else under .claude/ (settings.local.json, caches, whatever a daemon
# drops there) stays ignored, so the ambient-noise reason for the rule above is intact.
#
# This matters because the guard's second source is `git ls-files --others
# --exclude-standard`, which respects this file: an untracked .claude/ artifact would
# otherwise read as an unrelated changed file and fail a run for something the agent
# never did.
!.claude/hooks/
!.claude/settings.json
.copilot/
---
755  hooks/
644  settings.json  238B
---
.claude/hooks/README.md
.claude/hooks/opencode-review.sh
.claude/hooks/opencode-review.test.sh
.claude/settings.json
[0m
[0m$ [0mgit check-ignore -q .claude/hooks/opencode-review.sh 2>/dev/null; echo "hook: $?"; git check-ignore -q .claude/settings.json 2>/dev/null; echo "settings: $?"; git check-ignore -q .claude/settings.local.json 2>/dev/null; echo "local: $?"
hook: 1
settings: 1
local: 0
[0m
The diff I was given is a **stale/earlier version** — the on-disk `opencode-review.sh` has 212 lines (diff shows 167) and the test has 200 lines (diff shows 185). The on-disk versions contain a whole diff-inlining section and its test that are absent from the diff. I'll review the on-disk files as the real current state.
[0m→ [0mRead .claude/settings.json
[0m
[0m$ [0mls tasks/*/evaluator.sh tasks/*/verify-evaluator.sh 2>/dev/null | head -20
tasks/BE-001-customer-validation/evaluator.sh
tasks/BE-001-customer-validation/verify-evaluator.sh
tasks/BE-002-order-amount-validation/evaluator.sh
tasks/BE-002-order-amount-validation/verify-evaluator.sh
tasks/BE-003-confirm-shipment/evaluator.sh
tasks/BE-003-confirm-shipment/verify-evaluator.sh
[0m
[0m$ [0mbash .claude/hooks/opencode-review.test.sh 2>&1 | tail -40
ok    push, no changes                               exit 0, 0 reviewer call(s)
ok    gh pr create, no changes                       exit 0, 0 reviewer call(s)
ok    git status is not a push                       exit 0, 0 reviewer call(s)
ok    pushd is not a push                            exit 0, 0 reviewer call(s)
ok    gh pr view is not create                       exit 0, 0 reviewer call(s)
ok    push with a changed evaluator                  exit 0, 1 reviewer call(s)
ok    gh pr create, changed evaluator                exit 0, 1 reviewer call(s)
ok    reviewer argv                                  argv carries the file
ok    reviewer prompt                                the diff is inlined, not fetched
ok    the evaluator verifier is reviewed             exit 0, 1 reviewer call(s)
ok    BENCH_REVIEW_HOOK=0 disables it                exit 0, 0 reviewer call(s)
ok    no verdict                                     a verdict-less run is BLOCKED, not passed
ok    REJECT is advisory by default                  exit 0, 1 reviewer call(s)
ok    BENCH_REVIEW_STRICT=1 makes it L2              exit 3, 1 reviewer call(s)
ok    STRICT does not fail an ACCEPT                 exit 0, 1 reviewer call(s)
ok    a failing reviewer still exits 0               exit 0, 1 reviewer call(s)
ok    a README change is not reviewable              exit 0, 0 reviewer call(s)
ok    sample-service is out of scope                 exit 0, 0 reviewer call(s)
ok    empty stdin                                    exit 0, 0 reviewer call(s)
ok    not JSON                                       exit 0, 0 reviewer call(s)
ok    JSON without a command                         exit 0, 0 reviewer call(s)
ok    JSON, wrong shape                              exit 0, 0 reviewer call(s)
ok    opencode not installed                         exit 0, skipped with a reason
ok    the hook is committed                          tracked
ok    the wiring is committed                        tracked
ok    ambient .claude noise stays hidden             ignored

opencode-review.test: all 26 cases behaved as specified.
[0m
[0m$ [0mrtk git log --oneline -5 -- .claude/hooks/ 2>/dev/null; echo "---"; rtk git log --oneline -3 2>/dev/null
4e865b9 An evaluator that cannot fail a bad submission now gets read by a second model
---
987b059 The reviewer tried to test its own hook and was cut off mid-review
4e865b9 An evaluator that cannot fail a bad submission now gets read by a second model
8aadc75 Every fixture told the scorer which one it was (#21)
[0m
[0m$ [0mrtk git show 987b059 --stat 2>/dev/null | head -20
commit 987b0596ed1eddfdf3bb5607b27e34ee7748a3e7
Author: hermanngeorge15 <hermann.george15@gmail.com>
Date:   Thu Sep 3 09:10:26 2026 +0200

    The reviewer tried to test its own hook and was cut off mid-review
    
    Ran bench-critic against this very PR and it produced NO VERDICT. It was
    reasoning toward the right finding — whether these now-tracked .claude files
    appear in the scope guard when BASELINE_SHA predates them — and went to build a
    scratch repository under /tmp to confirm it. opencode auto-rejected that as an
    external-directory write and the run ended mid-sentence.
    
    By this hook's own rule that is BLOCKED, not a review. obs-critic died the same
    way on the sibling repo, and it is the same auto-rejection that cost codememory
    a silent review on its PR #102.
    
    The defect is in the prompt: it never said the reviewer cannot write outside the
    repository, so a reviewer inclined to test rather than read walks into a wall it
    cannot anticipate. It now says so, and redirects the instinct — write the probe
    out and label it reasoned rather than run — and says to emit the verdict line
[0m
[0m$ [0mrtk git show 987b059 -- .claude/hooks/opencode-review.sh 2>/dev/null | head -80
[0m
[0m$ [0mrtk git show 987b059 --stat 2>/dev/null
commit 987b0596ed1eddfdf3bb5607b27e34ee7748a3e7
Author: hermanngeorge15 <hermann.george15@gmail.com>
Date:   Thu Sep 3 09:10:26 2026 +0200

    The reviewer tried to test its own hook and was cut off mid-review
    
    Ran bench-critic against this very PR and it produced NO VERDICT. It was
    reasoning toward the right finding — whether these now-tracked .claude files
    appear in the scope guard when BASELINE_SHA predates them — and went to build a
    scratch repository under /tmp to confirm it. opencode auto-rejected that as an
    external-directory write and the run ended mid-sentence.
    
    By this hook's own rule that is BLOCKED, not a review. obs-critic died the same
    way on the sibling repo, and it is the same auto-rejection that cost codememory
    a silent review on its PR #102.
    
    The defect is in the prompt: it never said the reviewer cannot write outside the
    repository, so a reviewer inclined to test rather than read walks into a wall it
    cannot anticipate. It now says so, and redirects the instinct — write the probe
    out and label it reasoned rather than run — and says to emit the verdict line
    even when out of room, since a review that stops before its verdict is discarded
    whole.
    
    ON THE FINDING IT WAS REACHING FOR
    
    Checked by hand instead. With --baseline pointing at a commit that predates
    these files, `git diff --name-only $BASELINE_SHA -- .` does list
    .claude/hooks/opencode-review.sh and .claude/settings.json, neither matches an
    allowed prefix, and AC6 fails 21/F07 for something no agent did.
    
    It is real, and it is NOT a regression this PR introduces: it is true of every
    file any PR has ever added, since an old baseline diffs against a tree that
    lacks them. The case the ignore rule was actually written for — untracked
    ambient .claude noise reaching the guard through `ls-files --others` — still
    works, because settings.local.json remains ignored. Recorded here rather than
    fixed, because fixing it means changing what the evaluators measure.
    
    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01ESHcabCcFyTkNMRMu2RyCz

 .opencode/agent/bench-critic.md | 19 +++++++++++++++++++
 1 file changed, 19 insertions(+)
[0m
[0m$ [0mrtk git show 987b059 -- .opencode/agent/bench-critic.md 2>/dev/null
987b059 The reviewer tried to test its own hook and was cut off mid-review (63 minutes ago) <hermanngeorge15>
.opencode/agent/bench-critic.md | 19 +++++++++++++++++++
 1 file changed, 19 insertions(+)

.opencode/agent/bench-critic.md
  @@ -73,6 +73,25 @@ Read every task description as an adversarial reader would. If two competent eng
  +## Working constraints — read this before you plan a review
  +
  +**You cannot write files, and you cannot write outside this repository.** Attempting to build
  +a scratch git repository under `/tmp` to test a behaviour empirically will be auto-rejected as
  +an external-directory write, and the rejection has twice ended a review mid-sentence — leaving
  +no verdict, which is recorded as *no review having happened at all*.
  +
  +So do not plan to verify by experiment. Review by reading: the code, its tests, and `git show`
  +/ `git diff` / `git log` on this repository, which are read-only and permitted.
  +
  +When you would otherwise have run a probe, **write the probe out instead** — the exact commands
  +and the exact output you expect — and state plainly that you reasoned it rather than ran it.
  +A clearly-labelled analytical finding is worth far more than a blocked run, and an
  +over-confident finding you could not execute is worth less than nothing.
  +
  +**Emit the verdict line even if you run out of room.** A review that stops before its verdict
  +is discarded entirely, so if you are uncertain, say what you checked, say what you could not,
  +and still end with the line.
  +
   ## Output
   
   Markdown. One section per file. Under each, either `no finding` or numbered findings, each
  +19 -0
[0m
[0m✱ [0mGrep "ls-files --others|BASELINE_SHA|allowed prefix|AC6|scope guard"[90m in tasks · 61 matches[0m
[0m✱ [0mGrep "git diff --name-only|ls-files --others|allowed_prefix|ALLOWED_PREFIX"[90m in tasks/BE-001-customer-validation · 1 match[0m
Now let me look at the scope guard in the evaluators to understand the AC6 concern raised in the commit message, and check whether the hook's own self-review concern is real.
[0m→ [0mRead tasks/BE-001-customer-validation/evaluator.sh[90m [offset=85, limit=130][0m
[0m→ [0mRead tasks/BE-001-customer-validation/evaluator.sh[90m [limit=85, offset=1][0m
[0m
[0m$ [0mgit diff --name-only 8aadc75 -- . 2>/dev/null | head -20; echo "---untracked---"; git ls-files --others --exclude-standard 2>/dev/null | head -20
.claude/hooks/README.md
.claude/hooks/opencode-review.sh
.claude/hooks/opencode-review.test.sh
.claude/settings.json
.github/workflows/review-hook.yml
.gitignore
.opencode/agent/bench-critic.md
findings/opencode/.gitkeep
---untracked---
findings/opencode/review-20260903T075753Z.md
findings/opencode/review-20260903T081147Z.md
[0m
Now I understand the AC6 concern. Let me verify the key claim in commit 987b059 about whether `.claude/hooks/` files would trip AC6 when baseline predates them.
[0m$ [0mgit diff --name-only 8aadc75 -- . 2>/dev/null | rtk grep -E '\.claude/'; echo "---"; git ls-files --others --exclude-standard 2>/dev/null | rtk grep -E '\.claude/'
.claude/hooks/README.md
.claude/hooks/opencode-review.sh
.claude/hooks/opencode-review.test.sh
.claude/settings.json
---
[0m
[0m→ [0mRead .claude/hooks/opencode-review.sh[90m [offset=90, limit=50][0m
So the AC6 concern raised in commit 987b059 is real and confirmed: when an evaluator runs with `--baseline 8aadc75` (which predates these files), `git diff --name-only 8aadc75 -- .` lists `.claude/hooks/opencode-review.sh` and `.claude/settings.json`, neither matches the allowed production prefix `sample-service/src/main/kotlin/com/unityinflow/sample/customer/` nor the test prefix, so AC6 would fail with exit 21. But this is a pre-existing property of any newly-added file against an old baseline, not a regression this PR introduces — and the commit explicitly records it as "recorded here rather than fixed." Let me verify the IGNORE_RE doesn't catch `.claude/`:

The evaluator's `IGNORE_RE` is `(^|/)(target/|\.mvn/|\.git/)|\.(log|class|jar)$|^(run|evaluation)\.json$` — it does NOT exclude `.claude/`. So yes, `.claude/hooks/opencode-review.sh` would appear in `CHANGED_FILES` and fail AC6. But the `.gitignore` negation (`!.claude/hooks/`, `!.claude/settings.json`) means these are *tracked* files, so they only appear via `git diff --name-only` (the first source), not `ls-files --others` (the second source, which respects .gitignore). The commit's analysis is correct: this is a real interaction but pre-existing for any new tracked file, and the ignore rule still works for its actual purpose (untracked ambient noise).

Now let me focus on reviewing the actual changed files — the hook and its test — for the benchmark's integrity concerns.

Let me examine the key behaviors more carefully:
[0m
[0m$ [0mgit check-ignore -q findings/opencode/review-20260903T075753Z.md 2>/dev/null; echo "findings review: $?"; git check-ignore -q findings/opencode/.gitkeep 2>/dev/null; echo "findings gitkeep: $?"; rtk git ls-files findings/ 2>/dev/null
findings review: 1
findings gitkeep: 1
findings/opencode/.gitkeep
[0m
Now let me think about the key integrity concerns for this hook as a benchmark-protection mechanism. The hook is advisory (L3), not a gate. Let me check a few specific things:

1. The `findings/opencode/` directory — is it gitignored? It's in the diff output as `findings/opencode/.gitkeep`.
[0m$ [0mrtk grep -n 'findings' .gitignore 2>/dev/null; echo "---"; rtk git status --short findings/ 2>/dev/null
---
?? findings/opencode/review-20260903T075753Z.md
?? findings/opencode/review-20260903T081147Z.md
[0m
[0m$ [0mgit ls-files --others --exclude-standard 2>/dev/null | rtk grep -E 'findings/'
findings/opencode/review-20260903T075753Z.md
findings/opencode/review-20260903T081147Z.md
[0m
Interesting — `findings/` is NOT in `.gitignore`, so the review output files show up as untracked. But wait — the evaluator's `IGNORE_RE` doesn't match `findings/` either, and `ls-files --others --exclude-standard` would list them. Let me check:
[0m✱ [0mGrep "findings"[90m in tasks/BE-001-customer-validation · 0 matches[0m
[0m
[0m$ [0mrtk grep -rn 'findings' .gitignore tasks/ Makefile 2>/dev/null | head -20
(no output)
[0m
This is a real finding. The hook writes review output to `findings/opencode/review-*.md`, these files are untracked and NOT gitignored, so they appear in `git ls-files --others --exclude-standard` — the second source the evaluator uses to build `CHANGED_FILES`. They don't match `IGNORE_RE`, don't match the allowed production prefix, and don't match the test prefix, so **they would fail AC6 (exit 21) for any subsequent evaluation run in the same worktree**. The hook is creating the very kind of ambient noise that the `.gitignore` rules were written to prevent, but `findings/` was never added to `.gitignore`.

Let me verify this is actually reachable — would an evaluator run in a worktree where the hook has fired?
[0m$ [0mrtk grep -rn 'findings' .gitignore 2>/dev/null; echo "==="; rtk cat .gitignore 2>/dev/null | rtk grep -i finding
===
[0m
[0m$ [0mrtk grep -rn 'evaluator.sh\|BASELINE_SHA\|--baseline' Makefile .github/ 2>/dev/null | head -30
Makefile:30:	@$(BENCH_DIR)/verify-evaluator.sh
Makefile:35:	for script in tasks/*/verify-evaluator.sh; do \
Makefile:43:	@$(BENCH_DIR)/evaluator.sh
.github/workflows/verify-evaluator.yml:40:      # verify-evaluator.sh stages its own throwaway git repo from the working tree and
.github/pull_request_template.md:22:- [ ] `verify-evaluator.sh` still returns every registered exit code (6/6 for BE-003)
[0m
Confirmed: `findings/` is not in `.gitignore` at all. Now let me check whether the evaluator would actually be run in a worktree where the hook has fired. The hook fires on `git push` / `gh pr create` — these happen in the developer's worktree. The evaluator is also run in a worktree (the same one or a clean checkout). If the evaluator runs in the same worktree where the hook fired, the untracked `findings/opencode/review-*.md` files would be picked up by `ls-files --others --exclude-standard` and fail AC6.

But wait — let me check if the evaluator is typically run in a fresh clone/checkout where these files wouldn't exist. Let me look at how evaluation is invoked:
[0m→ [0mRead Makefile
`make evaluate` runs `evaluator.sh` in the current worktree. If a developer pushes (triggering the hook, which writes to `findings/opencode/`), then runs `make evaluate`, the untracked review files would be picked up by `ls-files --others --exclude-standard` and fail AC6. This is a real scenario — the hook creates files that contaminate the evaluator's scope guard.

However, I need to assess severity carefully. The hook is advisory and the review files are a developer convenience. The evaluator is typically run by a harness in a clean checkout, not in the developer's dirty worktree. But `make evaluate` exists precisely for running in the worktree, and the `.gitignore` already goes to great lengths to prevent exactly this class of contamination (`.DS_Store`, `.memdb/`, `.idea/`, `.vscode/`, `.claude/*`). The `findings/` directory is a new source of untracked files that the `.gitignore` rules were designed to prevent but missed.

Let me also check the `findings/opencode/.gitkeep` — it's tracked, so it's in the diff against an old baseline too:
[0m
[0m$ [0mrtk git diff --name-only 8aadc75 -- findings/ 2>/dev/null
findings/opencode/.gitkeep

Changes:
[0m
[0m$ [0mrtk grep -n 'findings\|gitignore\|IGNORE_RE\|AC6\|scope guard\|ls-files' .claude/hooks/opencode-review.test.sh 2>/dev/null
168:# --- the .gitignore negation, checked against THIS repo rather than the fixture
170:# `.claude/` is ignored here on purpose: the evaluator's scope guard collects untracked files
171:# with `git ls-files --others --exclude-standard`, so an ambient `.claude/settings.local.json`
172:# would otherwise read as an unrelated change and fail AC6 for something the agent never did.
[0m
So `findings/opencode/.gitkeep` is a tracked file added in this PR. Against an old baseline (8aadc75), it appears in `git diff --name-only` and would also fail AC6. But that's the same pre-existing property as `.claude/hooks/` — any new tracked file does this. The more concerning issue is the *runtime* contamination: the hook writes untracked files that aren't gitignored.

Now let me check the test file's coverage of this issue:
[0m→ [0mRead .claude/hooks/opencode-review.sh[90m [limit=20, offset=117][0m
The test checks the `.claude/` gitignore negation but does NOT check whether `findings/` is gitignored. The test is aware of the scope-guard contamination risk (it tests `.claude/settings.local.json` stays ignored) but misses that `findings/opencode/review-*.md` — the hook's own output — is not ignored.

Now let me look at other potential issues in the hook itself:

**Issue: `set -uo pipefail` without `set -e`** — The hook deliberately doesn't use `set -e` (it exits 0 on most failure paths). This is intentional and correct for an advisory hook. The `|| exit 0` and `|| true` patterns handle failures gracefully.

**Issue: the `case` glob matching for commands** — `*"git push"*` would match `git push` but also `echo "git push"` or any command containing that substring. Let me think about whether this is a problem:

- `git pushd` would NOT match `*"git push"*` because `pushd` has no space... wait, `git pushd` contains `git push` as a substring? No — `git pushd` is `git` + space + `pushd`. The glob `*"git push"*` looks for the literal string `git push` anywhere. `git pushd` contains `git push` (the first 8 chars of `pushd` are `push`... no, `pushd` is `p-u-s-h-d`, so `git pushd` contains `git push` as characters `git pu`... no. Let me be precise: the string `git pushd` = `g-i-t- -p-u-s-h-d`. The pattern `*"git push"*` = anything + `git push` + anything. `git push` = `g-i-t- -p-u-s-h`. In `git pushd`, after `git ` we have `pushd`, and `push` is a prefix of `pushd` but `git push` requires `git ` followed by `push` — and `git pushd` has `git ` followed by `pushd`, which contains `push` as a prefix. So `git pushd` DOES contain `git push` as a substring? `git pushd`[0:8] = `git push`. Yes! `git pushd` contains `git push` as a substring. So `git pushd something` would match and trigger a review.

But the test checks `pushd /tmp` (without `git` prefix) which correctly doesn't match. The test does NOT check `git pushd` which would be a false positive. However, `git pushd` is not a real git command, so this is a very minor false-positive risk. Let me check if there's a more realistic false positive... `git push --force` matches correctly. The real concern would be something like a script containing the string `git push` in a comment or echo, but the hook reads from the tool_input.command JSON field, which is the actual command being run, so this is unlikely to be a problem in practice.

**Issue: `origin/main` may not exist** — The hook uses `git merge-base HEAD origin/main`. If `origin/main` doesn't exist (e.g., fresh clone without remote, or remote uses `master`), `base` is empty and the hook exits 0 silently. This is handled correctly (line 92). The test sets up `refs/remotes/origin/main` explicitly.

**Issue: the diff truncation** — `MAX_DIFF_LINES` defaults to 1200. If the diff is truncated, the reviewer gets a partial diff. The prompt says "Read the files on disk for the rest." This is reasonable but the reviewer might miss issues in the truncated portion. This is a known tradeoff, not a defect.

**Issue: the verdict grep** — `grep -oE '^VERDICT: (ACCEPT|REJECT)'` uses `^` anchor. If the model writes `**VERDICT: ACCEPT**` (markdown bold), it won't match. The prompt says "End with a line 'VERDICT: ACCEPT'", so the model should output it plainly. But models sometimes add formatting. If the verdict is missed, the hook reports BLOCKED, which is the safe fallback. This is acceptable.

Let me now check one more thing — whether the hook's `findings/` output directory creation could fail silently and whether that's handled:
[0m→ [0mRead .claude/hooks/opencode-review.test.sh[90m [limit=30, offset=96][0m
If `mkdir -p findings/opencode` fails (e.g., read-only filesystem), `set -uo pipefail` without `set -e` means it continues. The `> "$out"` redirect would then fail, and with `set -o pipefail` but no `set -e`, the block's exit code is lost (it's a compound command, not in a pipeline). Actually, the `> "$out"` redirect failure would cause bash to error out of that redirect with an error message and non-zero exit, but since there's no `set -e`, the script continues to the `opencode run` line which also writes to `$out` (with `>>`). If `$out` can't be created, `opencode run >> "$out"` would also fail, the `if` catches it, and then the `grep -qE` on `$out` would fail (file doesn't exist), reporting BLOCKED. So the failure mode is safe — it degrades to BLOCKED. Good.

Now let me check the test more carefully for gaps:
Let me check the test's "reviewer prompt" assertion more carefully. The test checks `$CALLS` for `BEGIN DIFF` and `+exit 0`. But `$CALLS` records the stub's `$*` — the arguments passed to `opencode`. The stub does `printf '%s\n' "$(printf '%s' "$*" | tr '\n' ' ')" >> "$CALLS"`. So the entire prompt (including the inlined diff) is in `$CALLS`, with newlines replaced by spaces. The `grep -q 'BEGIN DIFF'` would find `--- BEGIN DIFF ---` (with spaces since newlines are replaced). And `grep -q '+exit 0'` would find the diff line `+exit 0` from the evaluator.sh content. This works.

But wait — the test checks `$CALLS` at line 107, which is the state after the "push with a changed evaluator" and "gh pr create, changed evaluator" runs (lines 89-90). The last run that wrote to `$CALLS` was "gh pr create, changed evaluator" which had 1 call. But the `run` function does `: > "$CALLS"` at the start, so `$CALLS` has the content from the last `run` call. The last run before line 107 is line 90 (`run "gh pr create, changed evaluator" "$PR" 0 1`). So `$CALLS` has one line with the prompt from that run. The prompt would contain the diff of `tasks/BE-001-x/evaluator.sh` which is `+exit 0`. This should work.

Now let me check a subtle issue — the test at line 107 runs AFTER the `run` at line 90, but the `run` function's `want_calls=1` means it already verified 1 call happened. The grep at 107 is an additional content check. But note: between line 90 and 107, there's the `grep -q 'tasks/BE-001-x/evaluator.sh' "$CALLS"` check at line 92. That check reads `$CALLS` which still has the content from the line 90 run. Then line 107 also reads the same `$CALLS`. This is fine — no intervening `run` call resets it.

Wait, actually line 92's check is between lines 90 and 107, and it doesn't reset `$CALLS`. But the `run` at line 90 set `: > "$CALLS"` then ran the hook which called the stub once, so `$CALLS` has one line. Lines 92 and 107 both read that same content. Correct.

Let me now check whether there's a race condition with the `stamp` in the output filename — if two hooks fire in the same second, they'd write to the same file:
[0m
[0m$ [0mrtk grep -n 'stamp\|date -u' .claude/hooks/opencode-review.sh 2>/dev/null
118:stamp="$(date -u +%Y%m%dT%H%M%SZ)"
119:out="findings/opencode/review-${stamp}.md"
122:  echo "# opencode review — ${stamp}"
[0m
[0m→ [0mRead .github/workflows/review-hook.yml
The timestamp is second-granularity. Two pushes in the same second would overwrite the same file. This is a minor issue — unlikely in practice, and the hook is advisory. Not a benchmark integrity concern.

Now let me check the `.github/workflows/review-hook.yml` that was also added (it shows in the diff against baseline):
[0m→ [0mRead .claude/hooks/README.md
This file is not in the diff I was given but it's in the changed files against baseline. It's a CI workflow that runs the hook's tests. It's path-scoped and looks correct. The comment says "22 cases" but the test actually has 26 cases (as shown by the test run output). This is a stale comment but not a functional issue.

Now let me check the `.claude/hooks/README.md` which is also tracked:
[0m
[0m$ [0mrtk cat .gitignore 2>/dev/null
target/
*.log
evaluation*.json
run*.json

# Ambient tooling artifacts. Background daemons, editors and indexers on a developer's
# machine write into whatever directory they are pointed at — including a benchmark
# worktree. Anything matched here is environment noise, not agent output, and must never
# be counted as a scope violation by the evaluator.
.DS_Store
.memdb/
.idea/
.vscode/
*.pid
*.swp
# Note the trailing `/*` rather than `/`: git will not re-include anything inside a directory
# it has already excluded, so `.claude/` plus `!.claude/hooks/` silently ignores the hook and
# leaves the repo looking as though it has a reviewer it never committed.
.claude/*
# ...except the committed review hook. These two are TRACKED, so the evaluator's scope guard
# sees them only when an agent actually edits them — which is a real violation and should
# fail AC6. Everything else under .claude/ (settings.local.json, caches, whatever a daemon
# drops there) stays ignored, so the ambient-noise reason for the rule above is intact.
#
# This matters because the guard's second source is `git ls-files --others
# --exclude-standard`, which respects this file: an untracked .claude/ artifact would
# otherwise read as an unrelated changed file and fail a run for something the agent
# never did.
!.claude/hooks/
!.claude/settings.json
.copilot/
[0m
Now I have a complete picture. Let me also verify the README's claim about `findings/` being filtered by rtk — it says `rtk git diff` filters `findings/` paths. But the `.gitignore` doesn't ignore `findings/`. The README mentions rtk filters it, but that's about the reviewer's `git diff` command, not about gitignore. The issue remains: the hook writes untracked files to `findings/opencode/` that are not gitignored.

Let me also check if there's a `findings/` entry I missed in .gitignore:
[0m$ [0mrtk grep -n 'BLOCKED\|empty.*diff\|diff.*empty\|came back empty' .claude/hooks/opencode-review.test.sh 2>/dev/null
125:if printf '%s' "$out" | grep -q 'BLOCKED'; then
126:  printf 'ok    %-46s a verdict-less run is BLOCKED, not passed\n' "no verdict"; PASS=$((PASS+1))
[0m
Confirmed: `findings/` is NOT in `.gitignore`. The `.gitignore` has extensive comments about preventing ambient noise from reaching the evaluator's scope guard, and carefully handles `.claude/`, `.DS_Store`, `.memdb/`, `.idea/`, `.vscode/`, etc. — but `findings/` is missing. The hook creates `findings/opencode/review-*.md` as untracked files, which will appear in `git ls-files --others --exclude-standard` and fail AC6.

This is the exact failure mode the `.gitignore` rules were designed to prevent: "an untracked artifact would otherwise read as an unrelated changed file and fail a run for something the agent never did." The hook is creating the very contamination it exists to protect against.

Let me now assess the severity. The scenario:
1. Developer works on a benchmark task in their worktree.
2. Developer pushes their branch — the hook fires, writes `findings/opencode/review-<timestamp>.md`.
3. Developer (or a harness) runs `make evaluate` in the same worktree.
4. The evaluator's `CHANGED_FILES` includes `findings/opencode/review-<timestamp>.md` (untracked, not ignored).
5. AC6 scope guard fails (exit 21) because `findings/opencode/review-*.md` doesn't match any allowed prefix.
6. The agent's submission is recorded as a scope violation (F07) for something the agent never did.

This is the harness blaming the agent for a harness fault — exit 21 instead of the correct verdict. It's the same class of failure the `.gitignore` rules exist to prevent, and the hook introduces a new source of it.

However — is this a defect in the *changed files under review* (the hook and its test), or is it a defect in `.gitignore` (which is also changed in this PR but not in the files I was asked to review)? The `.gitignore` is changed in this PR (it's in the diff against baseline), but I was only asked to review the two hook files. The `.gitignore` omission is the root cause, but the hook is what creates the files. Both are part of the same PR.

The hook itself should either:
1. Write to a gitignored location (but `findings/` isn't gitignored), or
2. The PR should have added `findings/` to `.gitignore` (it didn't), or
3. The evaluator's `IGNORE_RE` should exclude `findings/` (it doesn't).

The test file is aware of the scope-guard contamination risk (it tests `.claude/settings.local.json` stays ignored) but does not test that `findings/` is ignored — a gap in the test's coverage of its own hook's output.

Now let me also check one more thing — the `findings/opencode/.gitkeep` is tracked. Does it appear in the evaluator's `CHANGED_FILES`? It would only appear via `git diff --name-only $BASELINE_SHA` (since it's tracked), not via `ls-files --others`. So against an old baseline, `.gitkeep` would show up. But against a current baseline (where `.gitkeep` already exists), it wouldn't. The review-*.md files are the runtime concern.

Let me also verify: does the test actually test the BLOCKED-on-empty-diff path?
[0m$ [0mrtk grep -n 'opencode run\|opencode.*run' .claude/hooks/opencode-review.sh 2>/dev/null
143:# The cause was not the model. `opencode` runs its bash through an rtk rewrite plugin, and
190:if opencode run --agent "$AGENT" -m "$MODEL" "$prompt" >> "$out" 2>&1; then
[0m
The test checks the "no verdict" BLOCKED path but does NOT test the "empty diff" BLOCKED path (lines 159-164 of the hook). This is a gap — the hook has a specific BLOCKED path for when the diff comes back empty (the rtk filtering scenario), but the test doesn't exercise it. The test stubs `opencode` but doesn't stub `git diff`, so the diff always works in the test fixture. The empty-diff BLOCKED path is untested.

This is notable because the empty-diff BLOCKED path was added specifically to prevent the ten-minute loop that happened on 2026-09-03 — it's the fix for a real incident. But it's not tested. If someone accidentally removes lines 159-164, the test suite would still pass.

Let me verify this is actually untestable in the current fixture or just untested:

The test fixture creates real files and commits them, so `git diff` always produces output for changed files. To test the empty-diff path, you'd need to either:
- Stub `git diff` to return empty, or
- Create a scenario where the files match the globs but `git diff` returns empty (e.g., the files exist but haven't changed relative to the base)

The second scenario is hard to construct in the current fixture design. The first (stubbing git) would require a more complex test setup. So this is a coverage gap that's understandable but still a gap — the fix for a real incident is untested.

Now let me compile my findings. Let me also check one more subtle thing — the `opencode run` command and whether the stub correctly simulates it:
The hook calls `opencode run --agent "$AGENT" -m "$MODEL" "$prompt"`. The stub is named `opencode` and receives all args via `$*`. The stub doesn't check for `run` subcommand — it just records and prints a verdict. This is fine for testing purposes.

Let me now check one final thing — the `BENCH_REVIEW_STRICT` exit code of 3. The CLAUDE.md exit code table says 0/10/11/12/13/20/21/30. Exit 3 is not in that table. But the hook is not an evaluator — it's a review hook, and its exit codes are separate from the evaluator contract. The hook's own comments say "exit 3" for strict mode. The test verifies this (line 133: `run "BENCH_REVIEW_STRICT=1 makes it L2" "$PUSH" 3 1`). Exit 3 is a hook-specific code, not an evaluator code. This is fine.

Now let me write my review.

---

## `.claude/hooks/opencode-review.sh`

**Finding 1 — the hook writes untracked files that contaminate the evaluator's scope guard (AC6)**

Lines 117-119, 133, 162, 190: the hook writes review output to `findings/opencode/review-<timestamp>.md`. These files are untracked and `findings/` is NOT in `.gitignore` (confirmed by reading `.gitignore` and `git check-ignore`). The evaluator's scope guard (BE-001 line 97, BE-002 line 111, BE-003 line 117) builds `CHANGED_FILES` from two sources: `git diff --name-only $BASELINE_SHA -- .` and `git ls-files --others --exclude-standard`. The second source respects `.gitignore` — and since `findings/` is not ignored, the review files appear there.

Concrete scenario: a developer pushes their branch (hook fires, writes `findings/opencode/review-20260903T081147Z.md`), then runs `make evaluate` in the same worktree. The evaluator's `CHANGED_FILES` includes `findings/opencode/review-20260903T081147Z.md`, which matches neither the allowed production prefix (`sample-service/src/main/kotlin/com/unityinflow/sample/customer/`) nor the test prefix. AC6 fails with exit 21 (F07 — scope violation). The agent's submission is recorded as a scope violation for a file the agent never created.

This is the exact failure the `.gitignore` rules were written to prevent — the comment at `.gitignore` line 12 says "an untracked artifact would otherwise read as an unrelated changed file and fail a run for something the agent never did." The `.gitignore` handles `.DS_Store`, `.memdb/`, `.idea/`, `.vscode/`, `.claude/*` — but not `findings/`. The hook introduces a new source of the contamination those rules exist to prevent.

Severity: **high**. This produces a wrong exit code (21 instead of the correct verdict) and blames the agent for a harness fault — the most expensive kind of wrong this benchmark exists to catch. The fix is to add `findings/` to `.gitignore` (or have the hook write to a gitignored location), but neither was done in this PR.

**Finding 2 — the empty-diff BLOCKED path is untested**

Lines 159-164: the hook has a BLOCKED path for when `git diff` returns empty for matched files — the fix for the 2026-09-03 incident where `rtk git diff` filtered dotfile paths and the reviewer looped for ten minutes. The test file does not exercise this path. The test fixture creates real files and commits, so `git diff` always produces output; there is no stub for `git diff` and no scenario where matched files produce an empty diff. If lines 159-164 were accidentally removed, all 26 test cases would still pass.

Severity: **low**. The path is defensive and the scenario is environment-specific (rtk filtering), but it is the fix for a real incident and its absence from tests means a regression would go unnoticed.

## `.claude/hooks/opencode-review.test.sh`

**Finding 3 — the test does not assert that the hook's own output is gitignored**

Lines 168-192: the test has a section checking the `.gitignore` negation for `.claude/` paths — it asserts `.claude/hooks/opencode-review.sh` is tracked, `.claude/settings.json` is tracked, and `.claude/settings.local.json` is ignored. This is exactly the right kind of test for preventing scope-guard contamination. But it does not check `findings/` — the hook's own output directory. The test is aware of the contamination risk (the comment at line 170-172 explains it) but misses that `findings/opencode/review-*.md` is untracked and not ignored, creating the same contamination the `.claude/` rules prevent.

Concrete scenario: add `findings/` to `.gitignore` in a future PR, then accidentally remove it. No test would catch the regression, and the hook would silently start contaminating the evaluator again.

Severity: **low** (it is a test coverage gap, not a wrong exit code — but it leaves Finding 1 unguarded against future regression).

**Finding 4 — the `git pushd` false-positive is not tested**

Lines 84-86: the test checks `git status`, `pushd /tmp`, and `gh pr view 3` as non-matching commands. But the hook's glob `*"git push"*` (line 83) matches any command containing the substring `git push` — including `git pushd`, which contains `git push` as a prefix of `pushd`. The test does not check `git pushd`, so this false positive is unguarded. `git pushd` is not a real git command, so the practical risk is negligible, but a command like `echo "git push succeeded"` in a script would also match.

Severity: **low**. Unlikely to occur in practice and the consequence is a spurious review (advisory, exits 0), not a wrong benchmark result.

VERDICT: REJECT — Finding 1 is a high-severity defect: the hook writes untracked files to a non-gitignored directory, contaminating the evaluator's scope guard and producing exit 21 for files the agent never created, which is the exact harness-blames-agent failure this benchmark exists to catch.
